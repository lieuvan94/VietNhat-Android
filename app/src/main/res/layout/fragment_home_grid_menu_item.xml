<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/home_grid_item_area"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginStart="@dimen/dp_8"
        android:layout_marginEnd="@dimen/dp_8"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:duplicateParentState="true"
        android:gravity="center"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

    <RelativeLayout
            android:id="@+id/home_grid_menu_background"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:background="@drawable/circle_menu_white_bg"
            android:elevation="2dp">

        <ImageView
                android:id="@+id/home_grid_menu_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:adjustViewBounds="true"
                android:scaleType="fitXY"
                app:srcCompat="@drawable/ic_giaohang" />
    </RelativeLayout>

    <TextView
            android:id="@+id/home_grid_menu_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/home_grid_menu_background"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="8dp"
            android:includeFontPadding="false"
            android:text="@string/home_circle_menu_deliver"
            android:textColor="@color/color_indigo_26415d"
            android:textSize="@dimen/sp_14" />
</RelativeLayout>